<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysCourseResourceMapper">

    <resultMap id="SysCourseResourceResult" type="com.ruoyi.system.domain.SysCourseResource">
        <id property="resourceId" column="resource_id"/>
        <result property="courseId" column="course_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectCourseResourceById" resultMap="SysCourseResourceResult">
        select * from sys_course_resource where resource_id = #{resourceId}
    </select>

    <select id="selectCourseResourceList" resultMap="SysCourseResourceResult" parameterType="com.ruoyi.system.domain.SysCourseResource">
        select * from sys_course_resource
        where del_flag = '0'
        <if test="courseId != null and courseId != 0">
            and course_id = #{courseId}
        </if>
        <if test="deptId != null and deptId != 0">
            and dept_id = #{deptId}
        </if>
        <if test="fileName != null and fileName != ''">
            and file_name like concat('%', #{fileName}, '%')
        </if>
        order by create_time desc
    </select>

    <select id="selectResourcesByCourseId" resultMap="SysCourseResourceResult" parameterType="long">
        select * from sys_course_resource where del_flag = '0' and course_id = #{courseId} order by create_time desc
    </select>

    <insert id="insertCourseResource" parameterType="com.ruoyi.system.domain.SysCourseResource" useGeneratedKeys="true" keyProperty="resourceId">
        insert into sys_course_resource(course_id, dept_id, file_name, file_path, file_type, file_size, remark, create_by, create_time)
        values(#{courseId}, #{deptId}, #{fileName}, #{filePath}, #{fileType}, #{fileSize}, #{remark}, #{createBy}, sysdate())
    </insert>

    <update id="updateCourseResource" parameterType="com.ruoyi.system.domain.SysCourseResource">
        update sys_course_resource
        <set>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileType != null">file_type = #{fileType},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where resource_id = #{resourceId}
    </update>

    <delete id="deleteCourseResourceById" parameterType="long">
        update sys_course_resource set del_flag = '2' where resource_id = #{resourceId}
    </delete>

    <delete id="deleteCourseResourceByIds" parameterType="long">
        update sys_course_resource set del_flag = '2' where resource_id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
